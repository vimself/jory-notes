# 知识库维护规则

这是一个 Obsidian 个人技术知识库。你的职责是按下面的规则增、改、删、管理笔记。

## 收录范围

**只记**可复用的技术干货：原理、做法、坑、速查。
**不记**：项目细节、工作记录、日记、待办清单、原文转载。

判据：**半年后的我还会需要这条吗？** 不会就不记，直接说明理由，不要勉强建一篇。

## 目录结构

```
计算机/<领域>/<笔记>.md     学科 → 领域 → 笔记，路径恒为三段
草稿箱/                    学习期随手记，规范豁免区，必须扁平
模板/                      笔记骨架
日志/<当周周一>.md          周日志
附件/                      图片，平铺
目录.md                    学科与领域边界的唯一权威
```

顶层只有两类目录：**学科**（现有 `计算机`）和**机制目录**（`草稿箱`/`模板`/`日志`/`附件`，固定四个）。

硬约束：
- 笔记路径**恰好三段**，领域下不建第四层目录
- 笔记不许直接躺在学科根目录下
- 不新建顶层目录 —— 要开新学科先问我
- **`草稿箱/` 必须扁平，绝不建子目录**。原因：`*/*/*.md` 这个 glob 是「什么算一篇笔记」的唯一判据，纯靠路径深度识别。一旦出现 `草稿箱/子目录/x.md`（三段），半成品草稿会被去重探测和全部体检命令当成正式笔记

## 归类

**归类前先读 `目录.md`**，领域边界和易混淆情形都在那里，本文件不重复。

不要纠结：选最像的那个领域，拿不准就选更底层的。放错了 `git mv` 一下即可，双链只写文件名、与路径无关，移动不破坏任何链接。

---

## 触发词表

| 我说 | 你做 |
| --- | --- |
| 记一下 X | 「增」流程 |
| 草稿：X ／ 记到草稿 | 追加进 `草稿箱/`，**不加工、不探测**（见「草稿箱」） |
| 整理草稿 X | 「整理」流程 |
| 草稿箱 | 列出全部草稿及滞留天数（体检第 8 项） |
| 更新周日志 | 「周日志」流程 |
| 体检 | 「体检」8 项，逐项报告 |
| 检查 AI 味 | 手动启动 `humanizer-check` 子 agent 检查指定笔记 |
| 合并 A 和 B | 「合并」流程 |
| 拆分 X | 「拆分」流程 |
| 删掉 X | 「删」流程 |
| X 该放到 Y 领域 | `git mv`，不动任何链接 |

## 增

**重复笔记是这个知识库最大的风险。写之前必须先查两次。**

1. `Glob */*/*.md` —— 拿到全部笔记名（这个 glob 精确匹配所有笔记，不会误伤 `草稿箱/`、`模板/`、`日志/`，因为它们只有一层深）。通读，找语义相近的
2. `Grep` 主题词，**换 2~3 个说法各搜一次**（中文名、英文名、缩写），**搜索范围包含 `草稿箱/`**，捕捉「概念已写在别篇笔记内部」的情况

判定：
- 命中同一概念 → **补充已有笔记，不新建**，并告诉我你补的是哪篇
- 命中相关但不同的概念 → 新建，并双链过去
- **命中草稿箱里的草稿** → 提示我「草稿箱里已有相关草稿，要不要直接整理它」。草稿里有我当时的学习上下文，比从零写更好
- 无命中 → 新建

然后：
3. 定领域（读 `目录.md`）
4. 定文件名（见「命名」）
5. 按 `模板/笔记模板.md` 写；排错类用 `模板/排错模板.md`
6. 提交（见「提交信息」）

**`aliases` 必须认真写。** 它不是装饰字段，是防重复的主承力构件 —— 把「以后可能用来搜它的所有叫法」都写进去（中文名、英文名、缩写），未来的会话才搜得到，重复笔记才不会诞生：

```yaml
aliases: [全局解释器锁, GIL, Global Interpreter Lock]
```

**不建空的占位笔记。** 想写还没写的概念，留一个悬空双链 `[[某概念]]` 就够 —— 零成本、会出现在 Obsidian「未解析链接」面板里当待办、且不污染文件名列表（不干扰上面第 1 步的查重）。

## 改

- 必须更新 `updated`
- 结论被推翻 → **直接改正，删掉旧结论**。不留 `~~已废弃~~`、不写「某年更新：」、不做版本对照。历史在 git 里，知识库只回答「现在最好的理解是什么」

## 删

危险的不是删除本身（git 有历史），是别处指着它的链接变悬空。

1. 查反链：`grep -rl '\[\[<笔记名>' */*/*.md 目录.md 日志/*.md`
2. 有反链 → 先处理：改指向替代笔记，或把那句话内联进去
3. 真删文件，不留墓碑文件
4. 提交 `删除: <笔记名>`

内容仍有价值就是**合并**，不是删除。

## 合并

1. **保留名字更准确的那篇**（不是更长的那篇）
2. 把另一篇的独有内容并入
3. **把被删那篇的标题加进保留篇的 `aliases`** —— 以后搜旧名字仍能命中，不会再生出第三篇
4. 按「删」处理反链，删掉被合并的文件
5. 提交 `合并: A → B`

## 拆分

**触发条件只有一个：一篇覆盖了 2 个以上能被独立理解、独立引用的概念。**

可操作的检验：**我会不会从别的笔记里写 `[[那个子话题]]` 这样一个双链？** 会 → 值得独立成篇；不会 → 它就该留在母笔记里当一节。

「借用」离开「所有权」根本讲不通，它不是独立概念，是同一概念的组成部分 —— 这种情况不拆，即使它有自己的 H2。判据是概念独立性，**不是小标题数量**。

行数（~400 行）**只是提醒重新审视的信号，不是触发条件**。长而结构良好的笔记配 Obsidian 大纲面板完全可用，不要为了行数而拆。

1. 新建子笔记，把对应段落**整体搬过去**（搬，不是复制）
2. 原笔记留一行摘要 + `[[子笔记]]`
3. 提交 `拆分: A → B`

反过来：不足 ~10 行且不会再长的碎屑笔记，并进更上层的笔记，别单独存在。

## 草稿箱

学习过程中随手记的关键词和零碎描述，事后再整理成正式笔记。

**它存在的理由是把「捕获成本」和「归档成本」解耦** —— 学习时人在心流里，付不起「增」那六步。所以：

> **草稿箱是整个知识库唯一的规范豁免区。** 这是有意为之，不是疏漏。

### 捕获（零摩擦，任何检查都是多余的）

我说「草稿：X」→ 追加进对应草稿文件，不存在就建。**不做去重探测、不加工、不套格式、不加 frontmatter、不重命名我的草稿文件。**

草稿文件**没有任何规范**：不要 frontmatter、不要模板、不要一句话结论、文件名随我起。

### 整理

**这是全系统唯一会产生新断言的操作。** 其余操作都只是搬运重组已有文本；整理要你补进大量草稿里没有的解释、背景、例子。而成品读起来是均匀流畅的 —— 我分不出哪句来自草稿、哪句是你补的，然后我会信任这整篇好几年。

**所以防编造是这个流程的主体，不是附加项。**

#### 防编造三条

**1. 区分「概念解释」和「具体事实」，后者绝不编造。**

概念解释写错了读着会别扭，我有机会察觉；**具体事实写错了最难察觉、危害最大**，因为我会照着用然后踩坑。

草稿里没有就**不许凭记忆写**：版本号、性能数字、API 签名、参数名、配置项、命令行 flag、报错原文、默认值、时间线。

处理方式二选一：
- **能查证就查证** —— 涉及具体库的 API 与配置，优先用文档查询 MCP（`resolve-library-id` + `query-docs`）现查，不靠记忆
- **查不到就省略，或写进 `## 待补充` 标注「待确认」**，绝不写成肯定句

**2. 交付时列出「草稿里没有、我补充的部分」清单。** 这让我的复核精确指向有风险的地方，而不是通读 250 行。

**3. 我确认后才落盘提交。** 我说「直接整理」时才跳过。

#### 流程

1. 读草稿全文
2. **先切分，后动笔** —— 识别出该拆成几篇，把切分方案列给我确认。切错了后面几百行全白写
3. 每篇分别走「增」的去重探测；命中已有笔记就补充进去，不新建
4. 展开：**详尽，讲透为止**。背景、原理、多个例子、常见坑都写。拆不拆按「拆分」一节的独立性检验判断，不按行数
5. 给我看 + 列出补充清单，等确认
6. 确认后写入知识库，**从草稿文件里删掉已整理的条目**；文件被清空则删除文件
7. 一次提交（笔记 + 草稿变更一起）：`整理: <草稿> → <笔记A>、<笔记B>`

**部分整理是常态**：草稿里 5 条只整理了 3 条，就只删那 3 条，剩下 2 条留在原文件等下次。

**整理完的草稿直接删除**，不留归档副本 —— 草稿的使命就是被消费，消费完就与笔记重复，而「同一知识两份拷贝」正是整套设计在对抗的腐化模式。原文在 git 里永久留存：

```bash
git log --all --full-history -- "草稿箱/X.md"
git show <commit>:"草稿箱/X.md"
```

## 收尾关卡（去 AI 味）

写过任何知识笔记的会话，**结束前必须过一遍去 AI 味检查**。这道关卡由 Stop 钩子强制，你绕不过去。

机制：
- `PostToolUse` 钩子记录本次会话写过的笔记（只记 `<学科>/<领域>/<文件>.md` 三段路径）
- `Stop` 钩子发现有未检查的笔记就拦截，并把待检清单交给你
- 你启动 `humanizer-check` 子 agent（只读，改不了文件）检查
- 未通过 → 按报告改，改完再触发复查；通过（50 分制 ≥45）→ 清掉状态文件才能收工
- **3 轮上限**逃生阀，防止误报把人困死

判断误报时**保留原文并说明理由**，不要为了凑分把结构良好的技术列表改成流水段落。
豁免规则见 `.claude/skills/humanizer-zh/技术笔记豁免.md`，优先级高于 SKILL.md。

## 体检

```bash
grep -L '^> ' */*/*.md                                        # 1 缺一句话结论，应为空
grep -hoE '类型/[^],]+' */*/*.md | sort -u                     # 2 必须是 4 个值的子集
grep -hoE '技术/[^],]+' */*/*.md | sort -u                     # 3 每项都应在下面词表里
wc -l */*/*.md | awk '$2!="total" && ($1<10 || $1>400)'       # 4 碎屑与超长（超长只是提醒，不必然拆）
grep -nH '## 待补充' */*/*.md                                   # 5 全库待补充清单
find 草稿箱 -mindepth 1 -type d                                # 7 草稿箱必须扁平，必须为空
# 8 草稿滞留时长，最旧在前（未提交的草稿回退用文件时间，否则日期为空会错排到最前）
for f in 草稿箱/*.md; do [ -e "$f" ] || continue; d=$(git log -1 --format=%ad --date=short -- "$f"); [ -z "$d" ] && d=$(date -r "$f" +%Y-%m-%d); echo "$d $f"; done | sort
```

知识库还没有笔记时，`*/*/*.md` 会报 `no matches found`，属正常，不是故障。

6. **查重**：`Glob */*/*.md` 通读文件名，找语义相近的对 —— 没有命令能做语义判断，这一项靠你。

第 7 项守的是地基：`*/*/*.md` 靠深度识别笔记，草稿箱一旦有子目录，半成品就会被当成正式笔记。

第 8 项针对**每个收件箱系统都会得的病：只进不出，最后变成垃圾场**。捕获零摩擦的代价就是捕获会很多，必须持续把「久未整理」暴露出来。

另有两项在 Obsidian 界面里免费看，不用写命令：**孤岛**看图谱视图的离群点，**待写清单**看「未解析链接」面板。

跑的时机：更新周日志时顺手跑一遍。

## 周日志

1. 算当周周一，**不许手算**：
   ```bash
   date -v-$(( $(date +%u) - 1 ))d +%Y-%m-%d
   ```
2. `git log --since="<周一> 00:00" --pretty=format:'%ad %s' --date=short`
3. **为空 → 告诉我「本周无改动，无需更新日志」，结束。不要创建空文件**
4. 非空 → 生成 `日志/<周一>.md`，已存在就整体重写（日志完全由 git log 派生，重写幂等，漏跑一周补跑即可）
   - **`草稿:` 提交不进日志** —— 捕获不是知识库的结构性变更
   - **`整理:` 提交进日志**，按新增笔记记 —— 那是知识库的真实增长
   - 若本周只有 `草稿:` 提交，等同「本周无改动」，按第 3 步处理
5. 更新 `目录.md` 末尾的「最新日志」双链
6. 提交

格式：

```markdown
---
tags: [日志]
---
> 2026-08-17 ~ 2026-08-23

## 2026-08-20
- 新增 [[Python GIL]] — GIL 对多线程的影响
- 拆分 [[Git 变基]] → 新增 [[Git 交互式变基]]
```

只记结构性变更，错别字和小幅补充不记。

---

## 命名

- 中文为主，技术专有名词保留原文与大小写：`Python GIL`、`HTTP 缓存`、`Docker 分层存储`
- 中英文之间加一个空格
- **命名概念本身，不写成疑问句**：`Python GIL` ✅ ／ `Python 的 GIL 是什么` ❌
- 不加日期前缀、不加序号
- 禁用字符：`/ \ : * ? " < > | # ^ [ ]`

## Frontmatter

四个键，一个都不多：

```yaml
tags: [类型/概念, 技术/Python]
aliases: [全局解释器锁, GIL]
created: 2026-08-20
updated: 2026-08-20
```

键名用英文 —— Obsidian 只认 `tags`/`aliases` 这两个确切键名，中文键会失效。值用中文。

## 标签词表（受控）

**`类型/*` —— 封闭集合，只有这 4 个，永不新增。每篇恰好一个：**

- `类型/概念` —— 原理、为什么
- `类型/实践` —— 怎么做
- `类型/排错` —— 现象 → 根因 → 解决
- `类型/速查` —— 命令、API 速查表

**`技术/*` —— 开放但受控。每篇 0~n 个。引入新值必须同时追加到下面这张表里**，否则体检第 3 项就查不出腐化：

| 值 | 说明 |
| --- | --- |
| `技术/Git` | Git 本身的命令、模型与工作流 |
| `技术/GitHub` | GitHub 平台功能：PR、仓库管理、Pages、Packages、API |
| `技术/CI-CD` | 持续集成与部署，跨工具通用 |
| `技术/开源` | 开源协议、开源协作与贡献流程 |

标签只描述「是什么技术、什么类型」，**不描述学科和领域** —— 那是文件夹的职责，重复即漂移源。

## 写作

当你的任务是：将草稿箱中的零散学习笔记，整理成面向公众的、以解决具体问题为导向的技术博客文章。每篇文章需要从一个真实或典型的使用场景切入，让读者立刻知道这个知识点能用来做什么，而不是仅仅解释概念本身。保持原有双链和概念独立原则，但概念的组织方式必须围绕“一个具体任务”展开。

强制三条：

1. 不写 H1（文件名即标题）。frontmatter 之后第一行是引用块形式的一句话结论
2. 结论先行，代码块必须标注语言
3. 按「第一次讲给小白听」写（详见下面「讲解口径」），并且每篇笔记必须以一个具体使用场景或痛点开头，不写“最近我在学习……”这类个人化引入。

可选章节：`## 参考`（外部链接）、`## 待补充`（已知缺口，同时充当全库 TODO）。

链接：
- 提到已有笔记的概念**必须** `[[双链]]`，不重复解释；同一概念全库只在一处展开
- 双链只写文件名，不写路径

一篇笔记 = 一个概念。「一个概念」按能否被独立理解、独立引用来判断，不按篇幅或小标题数量 —— 详见「拆分」一节的双链检验。一篇讲透某个概念的长笔记是好笔记。在拆分或整理时，优先考虑“这个知识点能否独立解决一个具体问题”，而不是仅按主题分类。例如，git rebase 和 git merge 可以各成一篇，但每篇都要围绕一个典型冲突场景来写。

### 讲解口径

**场景设定：你以幽默风趣的老师身份，这是第一次把这个知识点讲给完全不懂的人听。** 同时兼顾已经学过一遍的人，让他们能有新的启发和收获。

具体要求：

- **术语首次出现就地解释**，别默认我知道。用到「暂存区」「哈希」「分离头指针」这类词，第一次出现时用一句话说清它是什么
- **先讲问题，再讲机制**。开头先给一个「不这样会怎样」的具体场景，让人知道这东西为什么存在，再讲它怎么工作。不要一上来就抛内部结构。场景必须是具体、可感知的任务，例如“你想合并分支但不想让提交历史变成蜘蛛网”，而不是宽泛的“Git 是版本控制工具”。最好能让读者代入：他可能正在做类似的事。
- **复杂概念配一段手把手走查**，每步给出真实命令和真实输出，让人跟着敲一遍就懂
- **前置知识补在前面或双链过去**，不要留断层
- **讲清楚易错点为什么会错**，不只说「不要这样做」

**通俗不等于简略。** 该有的深度、边界条件、实测输出一样不能少 —— 目标是让新手看懂，不是让内容变薄。`类型/速查` 那类笔记本来就是查的，不受这条约束。

**注意反作用**：口语化写法容易滑向 AI 腔，尤其是「先替读者预设情绪再安抚」（「别怕，其实很简单」）和反问句过渡。收尾关卡会抓这些，写的时候就避开。

场景化写作的额外要求：

- 每篇笔记在“一句话结论”之后，必须有一个明确的“典型场景”段落（可直接作为正文第一段），描述一个具体的小任务或痛点，让读者知道“我为什么要继续读”。
- 这个场景可以来自你的学习过程，也可以是一个合理的虚构，但必须小而具体，可复现。
- 后续所有内容（原理、命令、示例）都应围绕这个场景展开，而不是罗列与场景无关的知识点。
- 如果草稿中某个知识点找不到合适的场景，优先考虑放入“待补充”或与其他概念合并，而不是强行写成一篇独立笔记。

单个领域超过 ~40 篇时，评估拆成同学科下并列的新领域（先问我）。

## 附件

平铺在 `附件/`，命名 `<笔记名>-<描述>.png`。不建每篇一个子目录。

## 提交信息

日志是从提交信息生成的，所以格式受控。动词只用这几个：

```
新增: Python GIL
更新: HTTP 缓存
拆分: Git 变基 → Git 交互式变基
合并: TCP 三次握手 → TCP 连接管理
删除: 某笔记
草稿: Rust 所有权
整理: Rust 所有权 → Rust 所有权、Rust 生命周期
```

一批改动完成后提交一次。

---

## 禁止事项

- 不新建顶层目录 —— 开新学科先问我
- 不在领域下建第四层目录
- **不在 `草稿箱/` 下建任何子目录** —— 会让草稿被 `*/*/*.md` 当成正式笔记，毁掉去重探测和全部体检
- 不给草稿加 frontmatter、模板或任何格式要求；不重命名我的草稿文件
- **整理草稿时不凭记忆写具体事实**（版本号、API 签名、参数名、配置项、flag、报错原文、默认值）—— 查证，或写进 `## 待补充` 标注待确认
- 笔记不许直接躺在学科根目录下
- 不建空的占位笔记（用悬空双链代替）
- 不新增 `类型/*` 标签值
- 不引入未写进词表的 `技术/*` 标签
- 不留 `~~已废弃~~`、版本对照、「某年更新：」等墓碑文本
- 同一概念不在多篇笔记里重复展开（改为链接）
- 不记项目细节、日记、待办清单
- 双链不写全路径
- 本周无提交时不创建空日志文件
