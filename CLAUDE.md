# 知识库维护规则

Obsidian 个人技术知识库。你的职责是按下面的规则增、改、删、管理笔记。

## 收录范围

**只记**可复用的技术干货：原理、做法、坑、速查。
**不记**：项目细节、工作记录、日记、待办清单、原文转载。

判据：**半年后的我还会需要这条吗？** 不会就直接说明理由，不勉强建一篇。

## 目录结构

```
计算机/<领域>/<笔记>.md     学科 → 领域 → 笔记，路径恒为三段
草稿箱/                    学习期随手记，规范豁免区，必须扁平
模板/                      笔记骨架
日志/<当周周一>.md          周日志
附件/                      图片，平铺
目录.md                    学科与领域边界的唯一权威
```

顶层只有学科目录（现有 `计算机`）和固定四个机制目录（`草稿箱`/`模板`/`日志`/`附件`）。

**`草稿箱/` 必须扁平。** `*/*/*.md` 是「什么算一篇笔记」的唯一判据，纯靠路径深度识别；`草稿箱/` 下一旦出现子目录，半成品会被去重探测和全部体检当成正式笔记。

归类前先读 `目录.md`。不要纠结：选最像的领域，拿不准就选更底层的；放错了 `git mv` 一下即可，双链只写文件名、与路径无关。

## 触发词表

| 我说 | 你做 |
| --- | --- |
| 记一下 X | 「增」流程 |
| 草稿：X ／ 记到草稿 | 追加进 `草稿箱/`，**不加工、不探测** |
| 整理草稿 X | 「整理」流程 |
| 草稿箱 | 列出全部草稿及滞留天数（体检第 6 项） |
| 更新周日志 | 「周日志」流程 |
| 体检 | 「体检」7 项，逐项报告 |
| 检查 AI 味 | 启动 `humanizer-check` 子 agent 检查指定笔记 |
| 合并 A 和 B ／ 拆分 X ／ 删掉 X | 对应流程 |
| X 该放到 Y 领域 | `git mv`，不动任何链接 |

## 增

**重复笔记是最大的风险，写之前必须先查两次：**

1. `Glob */*/*.md` 拿到全部笔记名，通读找语义相近的
2. `Grep` 主题词，**换 2~3 个说法各搜一次**（中文名、英文名、缩写），**范围包含 `草稿箱/`**

判定：

- 同一概念 → **补充已有笔记，不新建**，并告诉我补的是哪篇
- 相关但不同 → 新建，双链过去
- 命中草稿箱 → 提示我「已有相关草稿，要不要直接整理它」
- 无命中 → 新建

然后：定领域（读 `目录.md`）→ 定文件名 → 按 `模板/笔记模板.md` 写（排错类用 `模板/排错模板.md`）→ 提交。

**`aliases` 必须认真写**，它是防重复的主承力构件——把以后可能用来搜它的所有叫法都写进去：`aliases: [全局解释器锁, GIL, Global Interpreter Lock]`

**不建空的占位笔记**：留一个悬空双链 `[[某概念]]` 就够，它会出现在 Obsidian「未解析链接」面板里当待办。

## 改

必须更新 `updated`。结论被推翻就**直接改正、删掉旧结论**——历史在 git 里，知识库只回答「现在最好的理解是什么」。

## 删

危险的不是删除本身（git 有历史），是别处的链接变悬空。

1. 查反链：`grep -rl '\[\[<笔记名>' */*/*.md 目录.md 日志/*.md`
2. 有反链先处理：改指向替代笔记，或把那句话内联进去
3. 真删文件，不留墓碑文件
4. 提交 `删除: <笔记名>`

内容仍有价值就是**合并**，不是删除。

## 合并

1. **保留名字更准确的那篇**（不是更长的那篇），并入另一篇的独有内容
2. **把被删那篇的标题加进保留篇的 `aliases`**——搜旧名字仍能命中，不会再生出第三篇
3. 按「删」处理反链，删掉被合并的文件
4. 提交 `合并: A → B`

## 拆分

**触发条件只有一个：一篇覆盖了 2 个以上能被独立理解、独立引用的概念。** 检验：**我会不会从别的笔记里写 `[[那个子话题]]`？** 会 → 独立成篇；不会 → 留作母笔记的一节。判据是概念独立性，**不是小标题数量、不是行数**（~400 行只是重新审视的提醒）。

1. 新建子笔记，把对应段落**整体搬过去**（搬，不是复制）
2. 原笔记留一行摘要 + `[[子笔记]]`
3. 提交 `拆分: A → B`

反过来：不足 ~10 行且不会再长的碎屑笔记，并进上层笔记。单个领域超过 ~40 篇时，评估拆成同学科下并列的新领域（先问我）。

## 草稿箱

**整个知识库唯一的规范豁免区**，用来把「捕获成本」和「归档成本」解耦。

**捕获**：我说「草稿：X」→ 追加进对应草稿文件，不存在就建。**不做去重探测、不加工、不套格式、不加 frontmatter、不重命名我的草稿文件。**

**整理**是全系统唯一会产生新断言的操作——你补的解释、背景、例子读起来和草稿原文一样自然，我分不出来，然后会信任它好几年。**所以防编造是这个流程的主体：**

- **具体事实绝不凭记忆写**：版本号、性能数字、API 签名、参数名、配置项、命令行 flag、报错原文、默认值、时间线。**能查证就查证**（具体库的 API 与配置优先用文档查询 MCP：`resolve-library-id` + `query-docs`）；**查不到就直接省略这一点**，绝不写成肯定句，也不留占位
- **交付时列出「草稿里没有、我补充的部分」清单**，让我的复核精确指向有风险的地方
- **我确认后才落盘提交**，我说「直接整理」时才跳过

流程：

1. 读草稿全文
2. **先切分，后动笔**——识别该拆成几篇，把方案列给我确认。切错了后面几百行全白写
3. 每篇分别走「增」的去重探测，命中已有笔记就补充进去
4. 展开：**详尽，讲透为止**，背景、原理、多个例子、常见坑都写
5. 给我看 + 列补充清单，等确认
6. 写入知识库，**从草稿文件里删掉已整理的条目**，文件清空则删除文件
7. 一次提交（笔记 + 草稿变更一起）：`整理: <草稿> → <笔记A>、<笔记B>`

**部分整理是常态**：5 条只整理了 3 条，就只删那 3 条。**整理完的草稿直接删除**，不留归档副本，原文在 git 里：`git log --all --full-history -- "草稿箱/X.md"`

## 收尾关卡（去 AI 味）

写过知识笔记的会话，结束前必须过一遍去 AI 味检查——Stop 钩子会拦住你并交出待检清单，照它的指示启动 `humanizer-check` 子 agent（只读，3 轮上限）。

判断误报时**保留原文并说明理由**，不要为了凑分把结构良好的技术列表改成流水段落。豁免规则见 `.claude/skills/humanizer-zh/技术笔记豁免.md`，优先级高于 SKILL.md。

## 体检

```bash
grep -L '^> ' */*/*.md                                    # 1 缺一句话结论，应为空
grep -hoE '类型/[^],]+' */*/*.md | sort -u                 # 2 必须是 4 个值的子集
grep -hoE '技术/[^],]+' */*/*.md | sort -u                 # 3 每项都应在标签词表里
wc -l */*/*.md | awk '$2!="total" && ($1<10 || $1>400)'   # 4 碎屑与超长（超长只是提醒）
find 草稿箱 -mindepth 1 -type d                            # 5 草稿箱必须扁平，必须为空
# 6 草稿滞留时长，最旧在前（未提交的草稿回退用文件时间）
for f in 草稿箱/*.md; do [ -e "$f" ] || continue; d=$(git log -1 --format=%ad --date=short -- "$f"); [ -z "$d" ] && d=$(date -r "$f" +%Y-%m-%d); echo "$d $f"; done | sort
```

7. **查重**：`Glob */*/*.md` 通读文件名找语义相近的对——没有命令能做语义判断，这一项靠你。

第 5 项守的是地基，第 6 项防「收件箱只进不出变垃圾场」。**孤岛**看图谱视图离群点、**待写清单**看「未解析链接」面板，在 Obsidian 里免费看。空库时 `*/*/*.md` 报 `no matches found` 属正常。跑的时机：更新周日志时顺手跑一遍。

## 周日志

1. 算当周周一，**不许手算**：`date -v-$(( $(date +%u) - 1 ))d +%Y-%m-%d`
2. `git log --since="<周一> 00:00" --pretty=format:'%ad %s' --date=short`
3. **为空 → 告诉我「本周无改动，无需更新日志」，结束，不创建空文件**
4. 非空 → 生成 `日志/<周一>.md`，已存在就整体重写（日志由 git log 派生，重写幂等）
   - **`草稿:` 提交不进日志**，**`整理:` 提交进**，按新增笔记记
   - 本周只有 `草稿:` 提交时等同「本周无改动」，按第 3 步处理
5. 更新 `目录.md` 末尾的「最新日志」双链，提交

只记结构性变更，错别字和小幅补充不记。格式：

```markdown
---
tags: [日志]
---
> 2026-08-17 ~ 2026-08-23

## 2026-08-20
- 新增 [[Python GIL]] — GIL 对多线程的影响
- 拆分 [[Git 变基]] → 新增 [[Git 交互式变基]]
```

## 命名

- 中文为主，技术专有名词保留原文与大小写：`Python GIL`、`HTTP 缓存`、`Docker 分层存储`
- 中英文之间加一个空格；不加日期前缀、不加序号
- **命名概念本身，不写成疑问句**：`Python GIL` ✅ ／ `Python 的 GIL 是什么` ❌
- 禁用字符：`/ \ : * ? " < > | # ^ [ ]`

## Frontmatter 与标签

四个键，一个都不多。键名用英文（Obsidian 只认 `tags`/`aliases` 这两个确切键名），值用中文。

```yaml
tags: [类型/概念, 技术/Python]
aliases: [全局解释器锁, GIL]
created: 2026-08-20
updated: 2026-08-20
```

**`类型/*` 是封闭集合，只有这 4 个，永不新增，每篇恰好一个：** `类型/概念`（原理、为什么）、`类型/实践`（怎么做）、`类型/排错`（现象 → 根因 → 解决）、`类型/速查`（命令与 API 速查表）。

**`技术/*` 开放但受控**，每篇 0~n 个，**引入新值必须同时追加到下表**，否则体检第 3 项查不出腐化：

| 值 | 说明 |
| --- | --- |
| `技术/Git` | Git 本身的命令、模型与工作流 |
| `技术/GitHub` | GitHub 平台功能：PR、仓库管理、Pages、Packages、API |
| `技术/CI-CD` | 持续集成与部署，跨工具通用 |
| `技术/开源` | 开源协议、开源协作与贡献流程 |

标签只描述「是什么技术、什么类型」，**不描述学科和领域**——那是文件夹的职责。

## 写作

笔记是面向公众的技术文章：**从一个具体场景切入，让读者立刻知道这个知识点能用来做什么**，而不是仅仅解释概念本身。

强制四条：

1. 不写 H1（文件名即标题），frontmatter 之后第一行是引用块形式的一句话结论
2. 紧跟一个**典型场景**段落：小而具体、可复现的任务或痛点，让读者知道「我为什么要继续读」。不写「最近我在学习……」这类个人化引入
3. 后续内容（原理、命令、示例）都围绕这个场景展开；结论先行，代码块必须标注语言
4. 按「第一次讲给小白听」写（见下）

**唯一可选章节是 `## 参考`（外部链接）。不写 `## 待补充`**——没查证、讲不透的点直接省略，不留缺口清单和「待确认」占位。

提到已有笔记的概念**必须** `[[双链]]`，不重复解释，同一概念全库只在一处展开；双链只写文件名。一篇笔记 = 一个概念，按「能否独立理解、独立引用、独立解决一个具体问题」判断，不按篇幅——讲透一个概念的长笔记是好笔记。找不到合适场景的知识点，并进相关笔记，不硬写成独立一篇。

### 讲解口径

**你以幽默风趣的老师身份，第一次把这个知识点讲给完全不懂的人听**，同时让学过一遍的人也有新收获。

- **术语首次出现就地解释**：「暂存区」「哈希」「分离头指针」这类词，第一次出现时用一句话说清
- **先讲问题，再讲机制**：先给「不这样会怎样」的具体场景（「你想合并分支但不想让提交历史变成蜘蛛网」，而不是「Git 是版本控制工具」），再讲它怎么工作
- **复杂概念配一段手把手走查**，每步给出真实命令和真实输出
- **前置知识补在前面或双链过去**，不留断层
- **讲清楚易错点为什么会错**，不只说「不要这样做」

**通俗不等于简略**——深度、边界条件、实测输出一样不能少（`类型/速查` 不受此约束）。**注意反作用**：口语化容易滑向 AI 腔，尤其是「先替读者预设情绪再安抚」（「别怕，其实很简单」）和反问句过渡，写的时候就避开。

## 附件与提交信息

图片平铺在 `附件/`，命名 `<笔记名>-<描述>.png`，不建子目录。

日志从提交信息生成，所以格式受控，动词只用这几个（一批改动完成后提交一次）：

```
新增: Python GIL
更新: HTTP 缓存
拆分: Git 变基 → Git 交互式变基
合并: TCP 三次握手 → TCP 连接管理
删除: 某笔记
草稿: Rust 所有权
整理: Rust 所有权 → Rust 所有权、Rust 生命周期
```

---

## 禁止事项

- 不新建顶层目录——开新学科先问我
- 不在领域下建第四层目录；笔记不许直接躺在学科根目录下
- **不在 `草稿箱/` 下建任何子目录**——会让草稿被 `*/*/*.md` 当成正式笔记，毁掉去重探测和全部体检
- 不给草稿加 frontmatter、模板或任何格式要求；不重命名我的草稿文件
- **不凭记忆写具体事实**（版本号、API 签名、参数名、配置项、flag、报错原文、默认值）——查证，或直接省略
- **不写 `## 待补充` 章节，也不留「待确认」「待实测」这类占位**
- 不建空的占位笔记（用悬空双链代替）
- 不新增 `类型/*` 标签值；不引入未写进词表的 `技术/*` 标签
- 不留 `~~已废弃~~`、版本对照、「某年更新：」等墓碑文本
- 同一概念不在多篇笔记里重复展开（改为链接）
- 不记项目细节、日记、待办清单
- 双链不写全路径
- 本周无提交时不创建空日志文件
