> 个人技术知识库。只收录可复用的技术干货：原理、做法、坑、速查。

判据一句话：**半年后的我还会需要这条吗？** 不会，就不记。

不收录：项目细节、工作记录、日记、待办清单、原文转载。

---

## 学科

顶层每个学科一个文件夹，笔记路径恒为 `<学科>/<领域>/<文件名>.md`，恰好三段。

| 学科 | 状态 |
| --- | --- |
| `计算机` | 主力 |
| 数学 | 未启用 |
| 金融 | 未启用 |

新学科由我决定何时开，开的时候一次性定好 2~4 个领域。

## 计算机 · 领域边界

**这张表是归类的唯一权威**，改了它，Claude 的归类行为立刻跟着变。

| 领域 | 收录 |
| --- | --- |
| `编程语言` | 语义、类型系统、内存模型、语言级并发（async/await、协程）、特性与惯用法 |
| `算法与数据结构` | 算法、数据结构、复杂度分析、算法思想 |
| `计算机系统` | 操作系统、进程与线程、内存管理、编译与链接、Linux、系统级并发原语（锁、原子操作） |
| `网络与协议` | TCP/IP、HTTP、TLS、DNS、RPC、网络排错 |
| `数据与存储` | 数据库、SQL、索引、事务、缓存、消息队列、文件与序列化格式 |
| `软件设计` | 架构、设计模式、分布式系统、API 设计、代码组织 |
| `开发工具链` | Git、Shell、Docker、K8s、CI/CD、构建工具、编辑器 |
| `人工智能` | LLM、Prompt、Agent、机器学习、AI 工程 |
| `前端与客户端` | 浏览器原理、CSS、前端框架、客户端与移动端 |

### 边界易混淆时

归类**不要纠结**——选最像的那个，拿不准就选更底层的。放错了 `git mv` 一下就好，双链只写文件名、与路径无关，移动不会破坏任何链接。

几个常见的分界：

- **并发**：`async`/协程/`GIL` 这类语言层面的 → 编程语言；线程、锁、内存屏障这类系统层面的 → 计算机系统
- **HTTP**：协议本身 → 网络与协议；缓存策略、REST/GraphQL 接口设计 → 软件设计
- **Docker**：怎么用、怎么写 Dockerfile → 开发工具链；容器隔离靠 namespace/cgroup 实现 → 计算机系统
- **SQL 优化**：索引与执行计划 → 数据与存储；分库分表架构 → 软件设计
- **Prompt / Agent**：→ 人工智能，即使它同时也是软件设计问题

---

## 其他目录

| 目录 | 用途 |
| --- | --- |
| `草稿箱/` | 学习时随手记关键词，事后说「整理草稿 X」变成正式笔记。**规范豁免区**：没有 frontmatter、没有模板、文件名随便起。**必须扁平，不建子目录** |
| `模板/` | 新建笔记的骨架，用 Obsidian「模板」插件快捷键插入 |
| `日志/` | 周日志，文件名 = 当周周一，内容由 git log 生成 |
| `附件/` | 图片等，平铺不分子目录 |

最新日志：[[2026-08-17]]

维护规则见 `CLAUDE.md`。
